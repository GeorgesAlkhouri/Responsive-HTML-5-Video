# Damn Cross Video Player

[Meteor](http://meteor.com/) based version. You need to install Meteor first: `curl https://install.meteor.com/ | sh`, checkout this repo, change into its folder and just run `meteor` to start the service.

